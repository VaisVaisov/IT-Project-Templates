FROM archlinux:latest

RUN sed -i '/NoExtract/d' /etc/pacman.conf && \
    pacman -Syu --noconfirm && \
    pacman -Sy --noconfirm \
        doxygen \
        git && \
    pacman -Scc --noconfirm

WORKDIR /docs

COPY docs/cpp/Doxyfile Doxyfile

CMD ["sh", "-c", "echo 'Используй: docker run --rm -v $(pwd):/docs docs-builder <команда>'; echo '  doxygen Doxyfile'"]
